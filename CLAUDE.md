# Agent instructions

Follow [AGENTS.md](./AGENTS.md). In particular, preserve the vocabulary in `CONTEXT.md`, keep tests independent of live credentials, run `make validate`, and never expose or persist secrets or provider content unintentionally.
